_target_: src.stage1.cosmos.two_dim_diffusion_slots.TwoDimDiffusionSlots
encoder_cfg:
  attn_resolutions: [32]
  channels: 128
  channels_mult: [2, 4, 4]
  dropout: 0.1
  in_channels: ${dataset.channels}
  spatial_compression: 8
  num_res_blocks: 2
  out_channels: 12
  resolution: ${dataset.dataset_names_to_img_size[${dataset.used}]}
  patch_size: 4
  decoder_patch_size: 4
  patch_method: "rearrange"
  latent_channels: 16
  z_channels: 16
  z_factor: 1
  name: "CIDiff"
  formulation: "AE"
  encoder: "Default"
  decoder: "Diffusion"
  time_scale: 1000.0
  z_cfg_drop: 0.1
  act_checkpoint: false
decoder_cfg:
  image_size: ${dataset.dataset_names_to_img_size[${dataset.used}]}
  in_channel: ${dataset.channels}
  inner_channel: 64
  out_channel: ${dataset.channels}
  res_blocks: 2
  attn_res: [32]
  z_channels: 16
  dropout: 0.1
  z_cfg_drop: 0.1
  channel_mults: [1, 2, 4, 4]
  conv_resample: true
  act_checkpoint: true
  num_heads: 12
  num_head_channels: -1
  num_heads_upsample: -1
  use_scale_shift_norm: true
  time_scale: 1000.0
  resblock_updown: true
  t_embed_type: cosine
  use_new_attention_order: false
enc_path: null
dec_path: null
use_repa: false
img_channels: ${dataset.channels}
enc_img_size: ${dataset.dataset_names_to_img_size[${dataset.used}]}
compile_model: false
learn_sigma: false
diffusion_type: fm
num_sampling_steps: ddim25
fm_sample_type: ode
quantizer_type:
quantizer_kwargs:
