_target_: src.stage2.cloud_removal.model.lightning_dit.LightningDiT_B_2
# CUHK-CR is RGB by default in this repo's litdata pipeline.
# Ensure this matches your cached litdata tensor channels.
in_channels: 3
# Must match your dataset image H/W (trainer expects fixed size here).
input_size: 256
cond_drop_prob: 0.0
qk_norm: false
